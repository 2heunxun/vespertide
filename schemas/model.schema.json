{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TableDef",
  "type": "object",
  "properties": {
    "columns": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ColumnDef"
      }
    },
    "constraints": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/TableConstraint"
      }
    },
    "description": {
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "$ref": "#/$defs/TableName"
    }
  },
  "required": [
    "name",
    "columns"
  ],
  "$defs": {
    "ColumnDef": {
      "description": "Definition of a single table column, including its type, nullability, and inline constraints.\n\nInline constraints (`primary_key`, `unique`, `index`, `foreign_key`) are the preferred way to\ndeclare constraints in model JSON files. Call [`TableDef::normalize`] to convert them into\ntable-level [`TableConstraint`] entries before diffing or SQL generation.\n\nUse [`ColumnDef::new`] to construct a column programmatically, then chain the setter methods\n(`.primary_key()`, `.unique()`, `.index()`, `.foreign_key()`, `.default()`, `.comment()`) to\nattach optional fields.\n\n[`TableDef::normalize`]: crate::schema::TableDef::normalize\n[`TableConstraint`]: crate::schema::TableConstraint",
      "type": "object",
      "properties": {
        "comment": {
          "type": [
            "string",
            "null"
          ]
        },
        "default": {
          "anyOf": [
            {
              "$ref": "#/$defs/DefaultValue"
            },
            {
              "type": "null"
            }
          ]
        },
        "foreign_key": {
          "anyOf": [
            {
              "$ref": "#/$defs/ForeignKeySyntax"
            },
            {
              "type": "null"
            }
          ]
        },
        "index": {
          "anyOf": [
            {
              "$ref": "#/$defs/StrOrBoolOrArray"
            },
            {
              "type": "null"
            }
          ]
        },
        "name": {
          "$ref": "#/$defs/ColumnName"
        },
        "nullable": {
          "type": "boolean"
        },
        "primary_key": {
          "anyOf": [
            {
              "$ref": "#/$defs/PrimaryKeySyntax"
            },
            {
              "type": "null"
            }
          ]
        },
        "type": {
          "$ref": "#/$defs/ColumnType"
        },
        "unique": {
          "anyOf": [
            {
              "$ref": "#/$defs/StrOrBoolOrArray"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "name",
        "type",
        "nullable"
      ]
    },
    "ColumnName": {
      "description": "The name of a table column, always in `snake_case` by convention.",
      "type": "string"
    },
    "ColumnType": {
      "description": "The SQL type of a column, either a parameter-free simple type or a parameterised complex type.\n\nIn JSON model files a simple type is written as a plain string (`\"integer\"`, `\"text\"`, etc.)\nwhile a complex type is written as an object with a `\"kind\"` discriminant\n(`{\"kind\": \"varchar\", \"length\": 255}`).\n\nAlways construct via the wrapped variants:\n```\nuse vespertide_core::{ColumnType, SimpleColumnType, ComplexColumnType};\nlet t1 = ColumnType::Simple(SimpleColumnType::Integer);\nlet t2 = ColumnType::Complex(ComplexColumnType::Varchar { length: 255 });\n```",
      "anyOf": [
        {
          "description": "A parameter-free SQL type such as `INTEGER`, `TEXT`, or `UUID`.",
          "$ref": "#/$defs/SimpleColumnType"
        },
        {
          "description": "A parameterised SQL type such as `VARCHAR(n)`, `NUMERIC(p,s)`, or a named enum.",
          "$ref": "#/$defs/ComplexColumnType"
        }
      ]
    },
    "ComplexColumnType": {
      "description": "Parameterised SQL column types that require additional configuration beyond a simple keyword.\n\nIn JSON model files these are written as objects with a `\"kind\"` discriminant, for example\n`{\"kind\": \"varchar\", \"length\": 255}` or `{\"kind\": \"enum\", \"name\": \"status\", \"values\": [...]}`.\n\nUse these via [`ColumnType::Complex`].\n\nThis enum is `#[non_exhaustive]`: new variants may be added in future releases.\nDownstream `match` expressions should include a wildcard arm.",
      "oneOf": [
        {
          "description": "Variable-length character string with a maximum byte length (`VARCHAR(n)`).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "varchar"
            },
            "length": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "length"
          ]
        },
        {
          "description": "Exact fixed-point number with configurable precision and scale (`NUMERIC(p, s)`).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "numeric"
            },
            "precision": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "scale": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "precision",
            "scale"
          ]
        },
        {
          "description": "Fixed-length character string padded with spaces (`CHAR(n)`).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "char"
            },
            "length": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "kind",
            "length"
          ]
        },
        {
          "description": "Escape hatch for database-specific types not covered by other variants.\nBreaks cross-database portability; avoid unless absolutely necessary.",
          "type": "object",
          "properties": {
            "custom_type": {
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "custom"
            }
          },
          "required": [
            "kind",
            "custom_type"
          ]
        },
        {
          "description": "Named enum type. String enums map to a native DB enum; integer enums store as `INTEGER`.\nSee [`EnumValues`] for the distinction.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "enum"
            },
            "name": {
              "type": "string"
            },
            "values": {
              "$ref": "#/$defs/EnumValues"
            }
          },
          "required": [
            "kind",
            "name",
            "values"
          ]
        }
      ]
    },
    "DefaultValue": {
      "description": "A column default value that can be a boolean, integer, float, or SQL expression string.\n\nIn JSON model files the `\"default\"` field accepts any of these forms:\n- `true` / `false` — boolean literal.\n- `0`, `42` — integer literal.\n- `0.0`, `1.5` — floating-point literal.\n- `\"'pending'\"` — SQL string literal (note the inner single quotes).\n- `\"NOW()\"` — SQL function call (no surrounding quotes).\n\nUse [`DefaultValue::to_sql`] to convert to the SQL representation for DDL generation.\n\n`StringOrBool` is a backwards-compatibility alias for this type.\n\nThis enum is `#[non_exhaustive]`: new variants may be added in future releases.\nDownstream `match` expressions should include a wildcard arm.",
      "anyOf": [
        {
          "description": "A boolean default (`true` or `false`).",
          "type": "boolean"
        },
        {
          "description": "An integer default value.",
          "type": "integer",
          "format": "int64"
        },
        {
          "description": "A floating-point default value.",
          "type": "number",
          "format": "double"
        },
        {
          "description": "A SQL expression or string literal default (e.g. `\"'pending'\"` or `\"NOW()\"`).",
          "type": "string"
        }
      ]
    },
    "EnumValues": {
      "description": "The set of allowed values for an enum column, either string-based or integer-based.\n\n**String enums** map to a native `PostgreSQL` `ENUM` type. Adding or removing values requires a\ndatabase migration (`ALTER TYPE`).\n\n**Integer enums** are stored as `INTEGER`. New variants can be added to the model without any\ndatabase migration because the underlying column type never changes.\n\nChoose integer enums for expandable value sets (roles, priorities) and string enums for\nstable, human-readable status fields.",
      "anyOf": [
        {
          "description": "String enum: each variant is a plain string stored in a native DB enum type.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "description": "Integer enum: each variant has an explicit numeric value stored as `INTEGER`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/NumValue"
          }
        }
      ]
    },
    "ForeignKeyDef": {
      "description": "Full foreign key definition used in the normalized table representation.\n\nSpecifies the referenced table and columns along with optional referential actions for\n`ON DELETE` and `ON UPDATE`. This is the canonical form produced after parsing any of the\nthree [`ForeignKeySyntax`] variants.\n\nAlways add `\"index\": true` on the column carrying the foreign key for query performance.",
      "type": "object",
      "properties": {
        "on_delete": {
          "description": "Action to take on child rows when the parent row is deleted.",
          "anyOf": [
            {
              "$ref": "#/$defs/ReferenceAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "on_update": {
          "description": "Action to take on child rows when the referenced column(s) in the parent row are updated.",
          "anyOf": [
            {
              "$ref": "#/$defs/ReferenceAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "ref_columns": {
          "description": "The column(s) in the referenced table that this foreign key points to.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ColumnName"
          }
        },
        "ref_table": {
          "description": "The table being referenced (the \"parent\" side of the relationship).",
          "$ref": "#/$defs/TableName"
        }
      },
      "required": [
        "ref_table",
        "ref_columns"
      ]
    },
    "ForeignKeySyntax": {
      "description": "Inline foreign key declaration on a [`ColumnDef`], supporting three levels of verbosity.\n\nIn JSON model files you can write:\n- `\"foreign_key\": \"user.id\"` (string shorthand)\n- `\"foreign_key\": {\"references\": \"user.id\", \"on_delete\": \"cascade\"}` (reference object)\n- `\"foreign_key\": {\"ref_table\": \"user\", \"ref_columns\": [\"id\"], \"on_delete\": \"cascade\"}` (full object)\n\nAll three forms are normalized into a [`ForeignKeyDef`] by the planner.\n\n[`ColumnDef`]: crate::schema::ColumnDef",
      "anyOf": [
        {
          "description": "Shorthand string in `\"table.column\"` format, e.g. `\"user.id\"`.",
          "type": "string"
        },
        {
          "description": "Object with a `\"references\"` key in `\"table.column\"` format plus optional actions.",
          "$ref": "#/$defs/ReferenceSyntaxDef"
        },
        {
          "description": "Full object with explicit `ref_table`, `ref_columns`, and optional actions.",
          "$ref": "#/$defs/ForeignKeyDef"
        }
      ]
    },
    "NumValue": {
      "description": "A single variant of an integer-backed enum, pairing a Rust-friendly name with its stored value.\n\nUsed inside [`EnumValues::Integer`] to define enums that are stored as `INTEGER` in the\ndatabase. Leave gaps between values (e.g. 0, 10, 20) so new variants can be inserted later\nwithout renumbering.",
      "type": "object",
      "properties": {
        "name": {
          "description": "The variant name used in generated code (e.g. `\"active\"`).",
          "type": "string"
        },
        "value": {
          "description": "The integer value stored in the database column.",
          "type": "integer",
          "format": "int64"
        }
      },
      "required": [
        "name",
        "value"
      ]
    },
    "PrimaryKeyDef": {
      "description": "Configuration for a primary key declared with the object syntax.\n\nWhen `auto_increment` is `true` the column is generated as a serial / identity column\n(`PostgreSQL` `SERIAL`, `MySQL` `AUTO_INCREMENT`, `SQLite` `AUTOINCREMENT`).\nUse [`PrimaryKeySyntax::Object`] to pass this to a [`ColumnDef`].\n\n[`ColumnDef`]: crate::schema::ColumnDef",
      "type": "object",
      "properties": {
        "auto_increment": {
          "description": "When `true`, the column value is generated automatically by the database on insert.",
          "type": "boolean",
          "default": false
        }
      }
    },
    "PrimaryKeySyntax": {
      "description": "Inline primary key declaration on a [`ColumnDef`], supporting both shorthand and full syntax.\n\nIn JSON model files you can write either:\n- `\"primary_key\": true` (shorthand, no auto-increment)\n- `\"primary_key\": {\"auto_increment\": true}` (full object syntax)\n\n[`ColumnDef`]: crate::schema::ColumnDef",
      "anyOf": [
        {
          "description": "Shorthand: `true` marks the column as a primary key without auto-increment.",
          "type": "boolean"
        },
        {
          "description": "Full object syntax allowing `auto_increment` to be configured.",
          "$ref": "#/$defs/PrimaryKeyDef"
        }
      ]
    },
    "ReferenceAction": {
      "description": "The referential action taken on child rows when the referenced parent row changes.\n\nUsed in `ForeignKeyDef::on_delete` and `ForeignKeyDef::on_update` to control cascading\nbehaviour. In JSON model files these are written in `snake_case`\n(e.g. `\"on_delete\": \"cascade\"`).\n\nThis enum is `#[non_exhaustive]`: new variants may be added in future releases.\nDownstream `match` expressions should include a wildcard arm.",
      "oneOf": [
        {
          "description": "Automatically delete or update child rows when the parent row is deleted or updated (`CASCADE`).",
          "type": "string",
          "const": "cascade"
        },
        {
          "description": "Prevent the parent row from being deleted or updated if child rows exist (`RESTRICT`).",
          "type": "string",
          "const": "restrict"
        },
        {
          "description": "Set the foreign key column(s) in child rows to `NULL` when the parent changes (`SET NULL`).\nThe column must be nullable.",
          "type": "string",
          "const": "set_null"
        },
        {
          "description": "Set the foreign key column(s) in child rows to their column default when the parent changes (`SET DEFAULT`).",
          "type": "string",
          "const": "set_default"
        },
        {
          "description": "Do nothing to child rows; the database defers enforcement or raises an error (`NO ACTION`).",
          "type": "string",
          "const": "no_action"
        }
      ]
    },
    "ReferenceSyntaxDef": {
      "description": "Compact foreign key syntax using a `\"references\"` string in `\"table.column\"` format.\n\nUseful when you only need to specify the target and optionally an `on_delete` action without\nlisting columns explicitly. The planner resolves the column from the `\"table.column\"` string.",
      "type": "object",
      "properties": {
        "on_delete": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReferenceAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "on_update": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReferenceAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "references": {
          "description": "Reference target in `\"table.column\"` format, e.g. `\"user.id\"`.",
          "type": "string"
        }
      },
      "required": [
        "references"
      ]
    },
    "SimpleColumnType": {
      "description": "Parameter-free SQL column types supported across all backends.\n\nEach variant maps directly to a standard SQL type. Use these via\n[`ColumnType::Simple`] when no length, precision, or scale is needed.\n\nThis enum is `#[non_exhaustive]`: new variants may be added in future releases.\nDownstream `match` expressions should include a wildcard arm.",
      "oneOf": [
        {
          "description": "16-bit signed integer (`SMALLINT`).",
          "type": "string",
          "const": "small_int"
        },
        {
          "description": "32-bit signed integer (`INTEGER`). Supports `auto_increment`.",
          "type": "string",
          "const": "integer"
        },
        {
          "description": "64-bit signed integer (`BIGINT`). Supports `auto_increment`.",
          "type": "string",
          "const": "big_int"
        },
        {
          "description": "32-bit floating-point number (`REAL`).",
          "type": "string",
          "const": "real"
        },
        {
          "description": "64-bit floating-point number (`DOUBLE PRECISION`).",
          "type": "string",
          "const": "double_precision"
        },
        {
          "description": "Unbounded Unicode text (`TEXT`).",
          "type": "string",
          "const": "text"
        },
        {
          "description": "Boolean true/false value (`BOOLEAN`).",
          "type": "string",
          "const": "boolean"
        },
        {
          "description": "Calendar date without time (`DATE`).",
          "type": "string",
          "const": "date"
        },
        {
          "description": "Time of day without date (`TIME`).",
          "type": "string",
          "const": "time"
        },
        {
          "description": "Date and time without timezone (`TIMESTAMP`).",
          "type": "string",
          "const": "timestamp"
        },
        {
          "description": "Date and time with timezone (`TIMESTAMPTZ`). Prefer this over `Timestamp`.",
          "type": "string",
          "const": "timestamptz"
        },
        {
          "description": "Time span / duration (`INTERVAL`).",
          "type": "string",
          "const": "interval"
        },
        {
          "description": "Variable-length binary data (`BYTEA`).",
          "type": "string",
          "const": "bytea"
        },
        {
          "description": "Universally unique identifier (`UUID`).",
          "type": "string",
          "const": "uuid"
        },
        {
          "description": "JSON value stored as text (`JSON`). Cross-backend compatible; prefer over `jsonb`.",
          "type": "string",
          "const": "json"
        },
        {
          "description": "IPv4 or IPv6 host address (`INET`). PostgreSQL-specific.",
          "type": "string",
          "const": "inet"
        },
        {
          "description": "IPv4 or IPv6 network address (`CIDR`). PostgreSQL-specific.",
          "type": "string",
          "const": "cidr"
        },
        {
          "description": "MAC address (`MACADDR`). PostgreSQL-specific.",
          "type": "string",
          "const": "macaddr"
        },
        {
          "description": "XML document (`XML`). PostgreSQL-specific.",
          "type": "string",
          "const": "xml"
        }
      ]
    },
    "StrOrBoolOrArray": {
      "description": "A JSON value that can be a string, an array of strings, or a boolean.\n\nUsed for inline `\"unique\"` and `\"index\"` declarations on a [`ColumnDef`]:\n- `true` / `false` — enable or disable the constraint with an auto-generated name.\n- A single string — the constraint name (used to group columns into a composite constraint).\n- An array of strings — a list of constraint names for multi-group membership.\n\nThis enum is `#[non_exhaustive]`: new variants may be added in future releases.\nDownstream `match` expressions should include a wildcard arm.\n\n[`ColumnDef`]: crate::schema::ColumnDef",
      "anyOf": [
        {
          "description": "A named constraint or group identifier.",
          "type": "string"
        },
        {
          "description": "Multiple constraint group names for multi-group membership.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "description": "`true` to enable with an auto-generated name; `false` to disable.",
          "type": "boolean"
        }
      ]
    },
    "TableConstraint": {
      "description": "A table-level constraint produced by [`TableDef::normalize`].\n\nInline column constraints (`primary_key`, `unique`, `index`, `foreign_key`) declared in model\nJSON files are converted into `TableConstraint` variants during normalization. You rarely\nconstruct these directly; the planner and SQL generator consume them.\n\nThis enum is `#[non_exhaustive]`: new variants may be added in future releases.\nDownstream `match` expressions should include a wildcard arm.\n\n[`TableDef::normalize`]: crate::schema::TableDef::normalize",
      "oneOf": [
        {
          "description": "Primary key constraint, optionally with auto-increment (serial / identity) semantics.",
          "type": "object",
          "properties": {
            "auto_increment": {
              "type": "boolean",
              "default": false
            },
            "columns": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ColumnName"
              }
            },
            "type": {
              "type": "string",
              "const": "primary_key"
            }
          },
          "required": [
            "type",
            "columns"
          ]
        },
        {
          "description": "Unique constraint ensuring no two rows share the same value(s) in the listed columns.",
          "type": "object",
          "properties": {
            "columns": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ColumnName"
              }
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "unique"
            }
          },
          "required": [
            "type",
            "columns"
          ]
        },
        {
          "description": "Foreign key constraint linking columns in this table to columns in another table.",
          "type": "object",
          "properties": {
            "columns": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ColumnName"
              }
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "on_delete": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ReferenceAction"
                },
                {
                  "type": "null"
                }
              ]
            },
            "on_update": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ReferenceAction"
                },
                {
                  "type": "null"
                }
              ]
            },
            "ref_columns": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ColumnName"
              }
            },
            "ref_table": {
              "$ref": "#/$defs/TableName"
            },
            "type": {
              "type": "string",
              "const": "foreign_key"
            }
          },
          "required": [
            "type",
            "columns",
            "ref_table",
            "ref_columns"
          ]
        },
        {
          "description": "Arbitrary SQL CHECK expression enforced by the database on every write.",
          "type": "object",
          "properties": {
            "expr": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "check"
            }
          },
          "required": [
            "type",
            "name",
            "expr"
          ]
        },
        {
          "description": "Non-unique index to speed up queries on the listed columns.",
          "type": "object",
          "properties": {
            "columns": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ColumnName"
              }
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "index"
            }
          },
          "required": [
            "type",
            "columns"
          ]
        }
      ]
    },
    "TableName": {
      "description": "The name of a database table, always in `snake_case` by convention.",
      "type": "string"
    }
  }
}